/**
 * Auth Index Route
 * 
 * This is the default route for the (auth) group.
 * It renders the LoginScreen component directly, making it
 * the first screen users see when accessing the auth group.
 */
import LoginScreen from './login';

export default LoginScreen; 